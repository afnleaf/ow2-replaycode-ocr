# finetune tesseract-ocr to overwatch font

we the base model data

we need the fonts

we need to isolate the letters and bounding boxes?

then train model

we need to containerize the training process.

```
Original Model Data -> FineTune -> New Model Data
```

then upload new model data to huggingface

then pull that model everytime we build the bot container
